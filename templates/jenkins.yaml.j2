jenkins:
  clouds:
    - docker:
        name: "docker"
        dockerApi:
          dockerHost:
            uri: "tcp://docker:2375"
        templates:
          - labelString: "{{ ludus_jenkins_docker_agent_label }}"
            dockerTemplateBase:
              image: "{{ ludus_jenkins_docker_agent_image }}"
            remoteFs: "/home/jenkins/agent"
            instanceCapStr: "10"
            retentionStrategy:
              dockerOnceRetentionStrategy:
                idleMinutes: 5
            connector:
              attach:
                user: "jenkins"
            numExecutors: {{ ludus_jenkins_docker_agent_executors }}
